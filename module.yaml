name: ocp-login
alias: cluster
type: terraform
description: Module to perform a simple cluster login and provide the cluster path
tags:
    - openshift
versions:
- platforms:
    - ocp4
  dependencies: []
  variables:
    - name: server_url
      scope: global
